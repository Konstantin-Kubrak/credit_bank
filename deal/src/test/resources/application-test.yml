spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        connection:
          isolation: 2
      jakarta:
        persistence:
          lock:
            timeout: 2000
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/changelog-master.xml